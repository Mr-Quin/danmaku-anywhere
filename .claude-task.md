# Task: Sakura — redesign Search page with chip-filtered list + close theme gaps

- **ClickUp ID**: DA-520 (subtask of DA-512 "Migrate popup to Sakura MUI theme")
- **Type**: extension
- **Branch**: `claude/danmaku-ui-redesign-ZqUaG` (REUSED — already pushed to origin with 1 commit on top of master)
- **Worktree**: `e:\danmaku-anywhere-DA-520`

## Context

This branch was created by a previous Claude session and already contains the Search page redesign work as a single commit on top of master:

```
92f12665 (extension) redesign Search page with chip-filtered list + close theme gaps
```

That commit:

- Bumps `shape.borderRadius` 8 → 12 in the Sakura theme (`src/common/theme/sakura.ts`); component overrides still pin 8/18 where appropriate.
- Adds `tooltipBg`/`tooltipFg` palette tokens; restyles `MuiTooltip`: caption variant, 8px radius, soft elevation, max-width 260.
- Rebuilds the Search page:
  - Mode toggle (Search Anime / Parse URL) → pill-segmented inside a `paperAlt` container.
  - New `SearchInput`: `paperAlt` field, leading search adornment, ⌘K hotkey hint (focuses input from anywhere in popup), primary focus glow. History dropdown preserved.
  - New `SourceFilterChips`: replaces the per-provider accordion. All enabled providers queried in parallel via `useQueries`; chips show counts when data resolves and the active chip drives the list.
  - New `SeasonResultRow` / `SeasonResultsList`: compact rows with 36×50 poster (gradient fallback), title, year · type · eps meta, downloaded checkmark. Replaces `SeasonGrid` card view on this page.
- Removes `useSeasonSearchSuspense`, `ProviderResultsList`, `SeasonSearchResult` (superseded).
- Adds i18n keys (en + zh).

Same `data-testid`s are preserved for e2e — only Search page look-and-feel is touched.

## Instructions

Execute the `da-dev` workflow starting from **step 3 (Implement)** in `.claude/commands/da-dev.md`. Steps 1–2 are already complete.

Specifically:

1. **Open dev browser** (extension change → required by da-dev step 4):

   ```bash
   wt -w 0 new-tab --title 'DA-520: dev browser' -d 'e:/danmaku-anywhere-DA-520/packages/danmaku-anywhere' -- powershell -NoExit -Command "pnpm dev:browser"
   ```

   `pnpm install` and `pnpm build:packages` already ran in this worktree.

2. **Review the existing commit critically.** It was authored by a prior Claude session without human review. Read the diff and decide whether the implementation actually matches the design intent and project conventions. Things to specifically check:
   - `data-testid`s preserved exactly so existing e2e specs still pass.
   - `useQueries` parallel fetching doesn't break the existing error-toast / loading semantics.
   - The ⌘K hotkey doesn't conflict with anything else in the popup.
   - Removed files (`useSeasonSearchSuspense`, `ProviderResultsList`, `SeasonSearchResult`) had no other call sites — grep across the whole monorepo, not just popup/.
   - `paperAlt` token exists in `sakura.ts` (the commit references it heavily).
   - History dropdown still functions in `SearchInput`.
   - `zh` translation entries actually translate (not just English copy).
   - The `borderRadius` bump 8 → 12 doesn't regress any other Sakura-themed surface.

3. **Run i18n extraction** if you touch any `t('...')` calls:

   ```bash
   cd e:/danmaku-anywhere-DA-520/packages/danmaku-anywhere && pnpm i18n extract
   ```

4. **Verify** per `packages/danmaku-anywhere/AGENTS.md`:
   - `pnpm type-check`, `pnpm lint`, affected tests
   - Manually exercise the Search page in the dev browser at 500×600 popup size: pill toggle, ⌘K, history dropdown, chip filter, SeasonResultRow click-through.

5. **Self-review** (da-dev step 5):
   - Run `/review` on the diff.
   - `/security-review` is optional here — this is theme + UI only, no auth/storage/network touched.

6. **Human gate** (da-dev step 6): This is a substantive UI change, not trivial. Summarize what you changed (vs the inherited commit), the review results, and dev-browser observations, then wait for explicit go before pushing.

7. **PR**:
   - Title: `(extension) redesign Search page with chip-filtered list + theme gaps [DA-520]`
   - Body bullets describe the changes; do NOT include the ClickUp URL — it's posted automatically.
   - Label: `ai-rereview`.

8. **Review monitoring** (da-dev step 8): start a `/loop 5m` once the PR is open.

## Notes

- The branch already tracks `origin/claude/danmaku-ui-redesign-ZqUaG`. When you push for the PR, push to the same remote branch — do not rename.
- env files (`.env`, `.env.local`) are already copied into this worktree.
- Parent task DA-512 is marked complete, but DA-520 is intentionally a follow-up subtask under that umbrella per user direction.
