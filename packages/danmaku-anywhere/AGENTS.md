# Agent context: packages/danmaku-anywhere

- **Purpose**: Chrome/Firefox extension that injects danmaku UI and uses content scripts + background to fetch and display danmaku on video pages.
- **Entry**: Extension entry points and Vite config; React app entry in `src/`.
- **Key areas**: Content scripts, popup/options UI, provider integration, danmaku engine usage. See repo rules `extension-standards.mdc` and `react-standards.mdc`.
- **When changing**: Update this file and `README.md` if you add scripts, major features, or new workspace dependencies.
