# Agent context: packages/bangumi-api

## Purpose

Typed Bangumi API schemas for use with openapi-fetch. No runtime code — build outputs type definitions only.

## Gotchas

- Run `pnpm generate:schemas` when upstream Bangumi OpenAPI specs change
- See `package.json` for available scripts and dependencies
